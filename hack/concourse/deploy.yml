platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang:1.10-stretch
    tag: latest

inputs:
- name: operator
- name: configs

run:
  path: operator/hack/concourse/deploy.sh
