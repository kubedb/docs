apiVersion: v1
kind: Secret
metadata:
  name: new-config
  namespace: demo
stringData:
  middleManagers.properties: |-
    druid.worker.capacity=5
  historicals.properties: |-
    druid.processing.numThreads=3
