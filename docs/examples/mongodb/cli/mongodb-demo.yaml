apiVersion: kubedb.com/v1
kind: MongoDB
metadata:
  name: mongodb-demo
  namespace: demo
spec:
  version: "8.0.17"
  storage:
    storageClassName: "standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
