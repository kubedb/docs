apiVersion: kubedb.com/v1
kind: MySQL
metadata:
  name: custom-mysql
  namespace: demo
spec:
  version: "9.6.0"
  configuration:
    secretName: my-custom-config
  storage:
    storageClassName: "standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
