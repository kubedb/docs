apiVersion: ops.kubedb.com/v1alpha1
kind: EtcdOpsRequest
metadata:
  name: etcdops-rotate
  namespace: demo
spec:
  type: ReconfigureTLS
  databaseRef:
    name: etcd-cluster
  tls:
    rotateCertificates: true
