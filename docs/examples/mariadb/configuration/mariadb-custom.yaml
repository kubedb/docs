apiVersion: kubedb.com/v1alpha2
kind: MariaDB
metadata:
  name: custom-mariadb
  namespace: demo
spec:
  version: "8.0.21"
  configSecret:
    name: my-custom-config
  storage:
    storageClassName: "standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
