apiVersion: kubedb.com/v1
kind: ProxySQL
metadata:
  name: custom-proxysql
  namespace: demo
spec:
  version: "3.0.1-debian"
  replicas: 1
  backend:
    name: my-group
  configuration:
    init:
      secretName: my-custom-config
