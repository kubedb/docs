apiVersion: kubedb.com/v1
kind: Elasticsearch
metadata:
  name: elasticsearch-demo
  namespace: demo
spec:
  version: xpack-9.2.3
  storage:
    storageClassName: "standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
